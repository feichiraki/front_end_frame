// setting子模块
const state = {
  theme: 'dark',
  desc: '测试demo',
}
const mutations = {}
const actions = {}
const getters = {}

// 导出模块
export default {
  // 开启命名空间
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
}
